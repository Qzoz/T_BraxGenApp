{
  "name": "tournament_brax",
  "version": "1.0.0",
  "description": "Tournament Brackets Generator Offline",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . brax-gen-app --overwrite --asar=true --platform=win32 --arch=x64 --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"qz-brax-gen\"",
    "package-lin": "electron-packager . brax-gen-app --overwrite --asar=true --platform=linux --arch=x64 --prune=true --out=release-builds"
  },
  "license": "ISC",
  "dependencies": {
    "sqlite3": "^4.1.1"
  },
  "devDependencies": {
    "electron": "^11.5.0",
    "electron-packager": "^14.2.1"
  }
}

